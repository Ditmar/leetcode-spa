import { Meta, Canvas, ArgTypes } from '@storybook/blocks';
import * as AvatarStories from './Avatar.stories';
import { Avatar } from './Avatar';

<Meta of={AvatarStories} />

# Avatar Component

The Avatar component is used to represent a user or entity. It consumes MUI's global design tokens for colors and standardized dimensions.

## Component API

The following table describes the public properties that the `Avatar` component accepts.

<ArgTypes of={Avatar} />

---

## Visual Showcase

This showcase demonstrates all the size variations and semantic colors supported by the component, arranged in a responsive column.

<Canvas of={AvatarStories.Showcase} />

## Default Variation

<Canvas of={AvatarStories.Default} />