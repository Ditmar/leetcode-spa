---
import Layout from '../layouts/Layout.astro';
import MyCustomProvider from '../components/MyCustomProvider';
import SmartHydrationComponent from '../components/SmartHydration';
---

<Layout>
  <main>
    <h1>Hidratación Inteligente</h1>

    <MyCustomProvider mode="ssr" client:load>
      <SmartHydrationComponent threshold={500} />
    </MyCustomProvider>

    <MyCustomProvider mode="client-only" client:idle>
      <SmartHydrationComponent threshold={2000} />
    </MyCustomProvider>
  </main>
</Layout>
